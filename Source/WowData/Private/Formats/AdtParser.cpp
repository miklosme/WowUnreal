#include "Formats/AdtParser.h"
DEFINE_LOG_CATEGORY_STATIC(LogAdt, Log, All);
FAdtData FAdtParser::Parse(const TArray<uint8>& Data, bool bBigAlpha)
{
    FAdtData Result;
    UE_LOG(LogAdt, Warning, TEXT("ADT parser stub - implementation pending"));
    return Result;
}
