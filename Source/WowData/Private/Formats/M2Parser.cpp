#include "Formats/M2Parser.h"
DEFINE_LOG_CATEGORY_STATIC(LogM2, Log, All);
FM2Data FM2Parser::Parse(const TArray<uint8>& InData, const TArray<uint8>& SkinData)
{
    FM2Data R;
    UE_LOG(LogM2, Warning, TEXT("M2 parser stub"));
    return R;
}
